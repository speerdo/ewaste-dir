
# Supabase — use new-format keys from dashboard (API Keys section)
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_PUBLISHABLE_KEY=   # formerly anon key (sb_publishable_... format)
SUPABASE_SECRET_KEY=               # formerly service_role key (sb_secret_... format) — server-only, never PUBLIC_
GOOGLE_PLACES_API_KEY=
GEMINI_API_KEY=
REVALIDATION_TOKEN=

# Admin — server-only, never PUBLIC_
ADMIN_PASSWORD=

# Email / notifications
PUBLIC_GOOGLE_MAPS_API_KEY=        # browser Maps — restrict to your domain in Google Cloud Console
SMTP_USERNAME=
SMTP_PASSWORD=
WEB3FORMS_ACCESS_KEY=
